database:
  adapter:  mysql
  host:     localhost
  username: taoyag
  password: taoyag
  database: test
  socket:   /opt/local/var/run/mysql5/mysqld.sock
  encoding: utf8

template_dir: template
out_dir: output

tables:
 - sample
 - sample2
 - users
 
